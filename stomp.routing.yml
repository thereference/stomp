stomp.connect:
  path: '/stom-connect'
  defaults:
    _controller: '\Drupal\stomp\Controller\StompController:connect'
    _title: 'Connect to Stomp'
  requirements:
    # Here is a comment why this open access callback is OK.
    _access: 'TRUE'

