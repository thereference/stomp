stomp.connect:
  path: '/stomp'
  defaults:
    _form: '\Drupal\stomp\Form\StompTestForm'
    _title: 'Connect to Stomp'
  requirements:
    # Here is a comment why this open access callback is OK.
    _access: 'TRUE'